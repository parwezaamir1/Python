from tkinter import *

root = Tk()
root.title("Login Form")
#root.iconbitmap('')   We can change the icon logo
root.minsize(400, 400)  # make the minsize of the window, we can also use maxsize 
root.configure(background='#0000FF')
root.mainloop()
